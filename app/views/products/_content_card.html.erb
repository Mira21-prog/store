<% @products.each do |product| %> 
	  <div class = "col-sm-4">
	  	<div class = "card card-focus">
		    <%= image_tag(product.image, size: "350x300", alt: "Image", class: "card-img-top") %><br>
		    <div class="card-body">
          <%= product.name %><br>
          <%= product.price %><br>
          <a href="#" class="btn btn-success">Buy</a>
          <div class = "characteristic">
          	<hr>
          	<%= product.characteristic %><br>
          </div>
        </div>
       </div>
	  </div>
<% end %>

<script>

</script>